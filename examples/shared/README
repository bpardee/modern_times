# Step 0
# Follow the directions for configuring jms.yml located in examples/README
# Perform steps 1-4 in separate terminals

# Step 1
# Start a JMS Server

# Step 2
# Start up the application
rm -f modern_times.yml
rackup -p 4567

# Step 3
# Browse to http://localhost:4567
# Open up the tree to ModernTimes => S1 => Attributes
# Enter 2 for count and click the Update button.
# Open up the tree to ModernTimes => S2 => Attributes
# Enter 3 for count and click the Update button.
# Refresh the browser so that you get the worker updates to the tree

# Step 4
# Publish 10 messages to the BarWorker and 5 to the BazWorker
jruby publish.rb 10 5

# Step 5
# cntl-c the rackup process and start it back up.  It should come back with
# the workers that have been configured via the browser

